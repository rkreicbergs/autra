// This is a generated file

#include "Game/Common/Register.h"

namespace AutraLib
{
    std::vector<Register> Register::RegisterPool;

    void Register::LoadData()
    {
        RegisterPool.push_back(Register(48, 60));
        RegisterPool.push_back(Register(60, 70));
        RegisterPool.push_back(Register(60, 72));
        RegisterPool.push_back(Register(57, 84));
        RegisterPool.push_back(Register(52, 78));
        RegisterPool.push_back(Register(45, 67));
        RegisterPool.push_back(Register(40, 67));
        RegisterPool.push_back(Register(40, 72));
        RegisterPool.push_back(Register(40, 84));
        RegisterPool.push_back(Register(40, 91));
        RegisterPool.push_back(Register(48, 72));
    }
}
